name: Reusable workflow from feature branch

on:
  workflow_call:
  push:
    branches:
      - main

jobs:
  api-tests:
    runs-on: ubuntu-latest
    steps:
      - id: step1
        run: echo "Running Reusable Workflow from Feature branch"
      - id: step2
        run: exit 0
